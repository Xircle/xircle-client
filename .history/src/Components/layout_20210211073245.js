import React from 'react';

const Layout = ({ children}) => {
  return (
    <>
        <header>

        </header>
        {children}
        <footer>
        </footer>
    </>
  );
}

export default Layout;
