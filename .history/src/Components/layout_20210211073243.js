import React from 'react';

const Layout = ({ children}) => {
  return (
    <>
        <header>

        </header>
        {children}
        <footer>
            
    </>
  );
}

export default Layout;
